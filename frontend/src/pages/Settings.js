import React from 'react';

export default function Settings() {
    return (
        <div className="settings-page card">
            <h1>Settings</h1>
            <p>Profile preferences, language, currency, and personalization options will appear here.</p>
        </div>
    );
}
